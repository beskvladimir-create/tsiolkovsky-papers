# Scans retrieved from the archive: 18 GB, and they are the archive's to
# publish rather than ours to redistribute.
data/

# Run logs and process state written by keeper.sh
*.log
.full_pid
.keeper_pid

# Output of classify_pages.py, regenerated in about 100 seconds
page_metrics.csv

__pycache__/
*.pyc
.DS_Store

# Статья не публикуется до подачи: приоритет за автором.
paper/
ПЛАН.md
ПИСЬМА_черновики.md
АНОНСЫ.md
